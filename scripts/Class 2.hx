package;

// Main from `source/`

class SuperMasha extends Main.Masha
{
    override function darDeComer()
    {
        super.darDeComer();

        trace('oso te voy a tirotear oso');

        trace(yaComio);
    }
}